<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.abc.zhor_main">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/zhor_main_ic_launcher"
        android:label="@string/zhor_main_s_app_name"
        android:roundIcon="@mipmap/zhor_main_ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/ZhorMainAppTheme"
        tools:replace="android:icon,android:label,android:roundIcon,android:supportsRtl,android:theme,android:allowBackup">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>